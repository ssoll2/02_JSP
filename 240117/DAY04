스크립트릿: html페이지 + 자바언어

<% int a= 10; %> => 자바입니다.
<% 자바코드; %> 단, 자바코드 안에 html 태그가 섞여있거나 코드 중간에 html를 쓸 경우에는
계속 <% %> 작업을 해줘야 하는 번거로움이 있다.

자바코드 : 비즈니스 코드
출력목적 <%= %>
<%
a() {
%>
<html>
<%
}
%>
실제 프로젝트를 할때는 간단한 수식또는 경로 정도에서만 사용하고 나머지 필요한 부분은 EL 또는 JSTL을 사용한다.

정리
<% 자바코드 %>
<%=값 또는 변수 %> 출력목적
<%! 선언 %>

submit : 해당 폼을 전송(action)한다.
서블릿, jsp, html,...

jsp -> jsp : MVC I(요즘에 거의 사용하지 않는다.)
control
view  model

view: 보여주는 코드(화면 ->jsp)
control : 서블릿(처리기)
model : 자료처리 or 자료조회 등등 -DB

Ex03_01 => view
쿼리 스트링 => view이름(파일이름).jsp?id=hong&pwd=...

jsp -> jsp => 일부분에서만 사용
jsp -> Servlet -> model한테 의뢰 -> return -> view

select 태그 안에 하위 태그는 option태그를 넣는다.

sido = "서울"
|
value = "서울" 의 글자가 넘어간다.
value값을 영어로 쓰는 것을 추천

조언~~
GPT 적극적으로 활용
GPT에 나온 결과값가지고 공부, 이해

선택한 값에 따라서 값이 달라지는 것이 실전코드

서울 클릭하면 각종 구가 나오는 것은 Script를 사용

문자가 일치하는지에 대한 확인은 .equals() 객체이기 때문에
이외는 == (비교연산자)를 이용

요청받은 객체에다가 더 추가하겠다. request.setAttribute
키: 값
key : value

RequestDisaptcher: 자바서블릿API에서 제공하는 인터페이스, 주로 서블릿간의 통신을 통해
클라이언트의 요청을 처리하는 데 사용 (즉, 전달 목적)
1.서블릿 간의 포워딩: 현재 실행중인 서블릿에서 다른 서블릿으로 요청을 전달
여러 서블릿이 협력하여 하나의 요청을 처리할 수 있다.
2. 서블릿에서 JSP로의 포워딩 : 서블릿이 비즈니스 로직을 처리한 후, 그 결과를 JSP페이지로 전달하여
동적인 웹페이지를 생성하는 데 사용. 서블릿은 JSP페이지로 제어를 전달하고, JSP페이지에서는 화면을 구성하고 응답을 생성
=> 안고 보내준다.
=> RequesDispatcher rd = request.getRequestDispatcher("Ex07.jsp")로 보낸다.
7번을 안고 간다.


페이지 이동 
1. foward 방식: rd.forward(request,response) 실제 페이지 이동(rd로 가라) 다 안고 감(모든 것)
2. redirect 방식: post 방식, response.sendRedirect("Ex08.jsp") 지가 스스로 응답, 페이지 이동이 주목적
=> 단순 페이지 이동이 목적(sendRedirect)
버튼하나에 서블릿이 만들어짐

.trim() : 걸러준다. 의무적으로 붙인다. 

RequestDispatcher 와 sendRedirect의 차이점: 둘다 서버 측 리다이렉션을 위한 메커니즘
RequestDispatcher: 기준이 서블릿
1. 서버내부 이동: 서버내에서 작동, 클라이언트에게는 변화가 없이 서버 내에서 다른자원(서블릿 또는 JSP)으로 요청전달
2. 현재 요청과 응답 객체를 공유, 데이터를 주고 받을 때
3. 서버 내부에서 작동하므로 클라이언트는 새로운 요청을 생성하지 않는다.
4. 클라이언트에게는 보이지 않음
SendRedirect
1. 맹목적인 이동. 새로운  URL로 즉시 이동, 새로운 요청 및 응답객체 사용, 클라이언트에게 노출됨
RequestDispatcher는 서버 내에서 작동, 클라이언트에게는 눈에띄지 않고 이동하는데 사용되고,
(내부 이동및 데이터 공유가 필요할 때)
SendRedirect는 클라이언트의 브라우저를 통해 외부로 이동하므로, 서버간의 외부 리다이렉션이 이루어진다.
나혼자 외부에서 움직임(외부로의 이동이 필요할때)



























